function on_resize(a, b) {
    return onresize = function() {
        clearTimeout(b), b = setTimeout(a, 100);
    }, a;
}

!function(a, b) {
    b.getActiveMQ = function() {
        a('<div id="getActiveMQ-watcher"></div>').appendTo("body").hide();
        var c = b.getComputedStyle, d = document.getElementById("getActiveMQ-watcher");
        return b.getActiveMQ = "currentStyle" in d ? function() {
            return d.currentStyle.fontFamily.replace(/['"]/g, "");
        } : c ? function() {
            return c(d, null).getPropertyValue("font-family").replace(/['"]/g, "");
        } : function() {
            return "unknown";
        }, b.getActiveMQ();
    };
}(jQuery, window), on_resize(function() {
    console.log("breakpoint is " + getActiveMQ());
})();